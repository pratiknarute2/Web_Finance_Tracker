{
    "name": "practice-expense-tracker",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "test": "npx playwright test",
        "test:headed": "npx playwright test --headed",
        "lint": "npx eslint ."
    },
    "devDependencies": {
        "@playwright/test": "^1.57.0"
    }
}